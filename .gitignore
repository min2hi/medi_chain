# Node Global
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# IDE / Editor General
.idea/
.vscode/
*.swp
*.swo

# ── Local-only: AI Tools & Agent Config ─────────────────────────────────────
# Mỗi developer dùng AI tool khác nhau — không enforce lên team
.agent/           # Antigravity IDE skills & rules
.gemini/          # Antigravity personal settings
.claude/          # Claude/AI agent config

# AI onboarding doc — personal, không phải team contract
AGENTS.md

# ── Local-only: Git Hooks ────────────────────────────────────────────────────
# Mỗi dev tự quyết định dùng hook không — không enforce
# Hook chỉ có tác dụng khi npm install trên máy cá nhân
.husky/

# ── Local-only: Internal Docs & Playbooks ───────────────────────────────────
# ADR, retros, playbooks — tài liệu nội bộ, không phải source code
# Contributor chỉ cần đọc README.md để bắt đầu
docs/

# ── Local-only: AI Evaluation & Dev Tools ───────────────────────────────────
# promptfoo, eval scripts — chỉ dùng khi dev AI features
ai-evals/

# ── Rác bị commit nhầm ──────────────────────────────────────────────────────
# Hugging Face model cache — không phải source code
undefined/

# OS Data
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# Environment variables (Global Safety Net)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.production   # ← File secrets thật — TUYỆT ĐỐI KHÔNG commit

# Logs
logs/
*.log
logs.txt
out.log

# Personal / Docs files
*.docx
*.zip
*.rar
*.pdf

# Conflict resolved files
*.resolved

# Presentation & scratch files (không phải source code)
"presentation 2"/
/presentation*/

# Test scripts (one-off)
test-*.js
test-*.ts
update-*.ts

# Micro-Swarm Simulator (local dev tool only — KHÔNG commit lên Git)
simulator/

# MiroFish — External AI simulation tool, NOT part of MediChain source
# Tool riêng biệt, config và data hoàn toàn độc lập
MiroFish/

# TypeScript build info
*.tsbuildinfo

.gitnexus
