# Node Global
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# IDE / Editor General
.idea/
.vscode/
*.swp
*.swo

# AI IDE & Agent Config — Tool-specific, không push lên Git
# Chỉ docs/ mới là nguồn truth cho team
.agent/           # Antigravity IDE skills & rules
.gemini/          # Antigravity personal settings
.claude/          # Claude/AI agent config — giữ local
AGENTS.md         # AI agent instruction file — personal, không phải team docs

# OS Data
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# Environment variables (Global Safety Net)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.production   # ← File secrets thật — TUYỆT ĐỐI KHÔNG commit

# Logs
logs/
*.log
logs.txt
out.log

# Personal / Docs files
*.docx
*.zip
*.rar
*.pdf

# Conflict resolved files
*.resolved

# Presentation & scratch files (không phải source code)
"presentation 2"/
/presentation*/

# Test scripts (one-off)
test-*.js
test-*.ts
update-*.ts

# Micro-Swarm Simulator (local dev tool only — KHÔNG commit lên Git)
simulator/

# MiroFish — External AI simulation tool, NOT part of MediChain source
# Tool riêng biệt, config và data hoàn toàn độc lập
MiroFish/

# TypeScript build info
*.tsbuildinfo

.gitnexus
